<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:id="@+id/listeningContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="24dp">

        <com.hwanghj09.sonju.voice.VoiceWaveformView
            android:id="@+id/listeningWaveform"
            android:layout_width="168dp"
            android:layout_height="80dp"
            android:importantForAccessibility="no" />

        <TextView
            android:id="@+id/listeningStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:accessibilityLiveRegion="polite"
            android:gravity="center"
            android:includeFontPadding="false"
            android:minLines="1"
            android:text="@string/voice_listening"
            android:textColor="@color/white"
            android:textSize="28sp"
            android:textStyle="bold" />
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/cancelListeningButton"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="112dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="28dp"
        android:contentDescription="@string/voice_cancel_description"
        android:text="@string/voice_cancel"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="18sp"
        app:cornerRadius="28dp"
        app:rippleColor="#33FFFFFF" />
</merge>
